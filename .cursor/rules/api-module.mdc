---
trigger: 用户要求创建新的 API 功能/模块
---

当用户要求创建新的 API 功能时，按以下步骤执行：

## 步骤

1. **确认需求** - 确认字段、验证规则、业务逻辑

2. **创建 Model** (`models/<name>.py`)
   - 继承 Base
   - 定义表名和字段
   - 添加时间戳

3. **创建 Schema** (`schemas.py`)
   - `<Name>Create` - 创建请求
   - `<Name>Update` - 更新请求
   - `<Name>Response` - 响应 (配置 from_attributes)

4. **创建 Router** (`routers/v1/<name>.py`)
   - CRUD 端点
   - 使用 pagination 工具
   - 异常处理

5. **注册路由** (`routers/v1/__init__.py`)
   - import router
   - app.include_router

6. **创建测试** (`tests/test_<name>.py`)
   - 使用内存 SQLite
   - 测试 CRUD
   - 测试边界情况

7. **生成迁移**
   ```bash
   uv run alembic revision --autogenerate -m "add <name> table"
   ```
